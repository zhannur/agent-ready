[project]
name = "agent-ready"
version = "0.1.0"
description = "Can a clean agent finish your quickstart? Clean-sandbox SDK acceptance runs with scored evidence."
requires-python = ">=3.11"
dependencies = [
    "daytona",
    "braintrust",
    "openai",
    "httpx",
    "pyyaml",
]

[tool.uv]
package = false
