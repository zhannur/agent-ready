name: braintrust
quickstart_url: https://www.braintrust.dev/docs/start
promised_output: "An eval run is executed via the SDK and its scores are reported"
sandbox_env:
  BRAINTRUST_API_KEY: $BRAINTRUST_API_KEY
